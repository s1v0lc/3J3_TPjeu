using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class toDoList : MonoBehaviour
{
    // Choses à faire //
    // pour le 12 nov //
    /*
        -> Lumières (lampadaires, phares)
        -> Colliders (côtés, derrière)
        -> Difficulté & vies
        + Fichiers texte expliquant mes objectifs
    */
    // Pour le 12 déc //
    /*
        -> Scène d'intro (controles, UI, droits d'auteurs)
        -> UI (vitesse, score)
        -> Scène de game over
        -> Sons (voitures, crash, ambulance, musique)
        -> Plus de tiles de routes
    */
}
